# Runtime deps. The local CLI (python Triage.py ...) only needs pydantic; the
# rest are for the hosted web app (azure_app/main.py) - Vercel installs this
# file, and `anthropic` powers its API backend.
pydantic>=2.0.0
anthropic>=1.0.0
fastapi>=0.115.0
python-multipart>=0.0.9
